<style>
    .input-group-text {
        border-radius: 0 0.35rem 0.35rem 0 !important;
        border-left: none !important;
    }
</style>
{{ form_start(form) }}
<div class="form-row">
    <div class="col">
        {{ form_row(form.title) }}
    </div>
    <div class="col">
        {{ form_row(form.monthNbr) }}
    </div>
    <div class="col">
        {{ form_row(form.price) }}
    </div>
</div>
<button class="btn btn-primary"><i class="fas fa-save"></i> {{ button_label|default('تسجيل') }}</button>
{{ form_end(form) }}
