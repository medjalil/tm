{% extends 'base.html.twig' %}

{% block title %}إسترجاع كلمة المرور{% endblock %}

{% block login %}
    <div class="container">
        <!-- Outer Row -->
        <div class="row justify-content-center">
            <div class="col-xl-10 col-lg-12 col-md-9">
                <div class="card o-hidden border-0 shadow-lg my-5">
                    <div class="card-body p-0">
                        <!-- Nested Row within Card Body -->
                        <div class="row" style="height: 500px;">
                            <div class="col-lg-6" style="background: #e6effd;">
                                <div class="p-5 mt-5">
                                    <h1>إسترجاع كلمة المرور</h1>
                                    {% for flashError in app.flashes('reset_password_error') %}
                                        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
                                    {% endfor %}
                                    <div class="mt-3">
                                        {{ form_start(requestForm) }}
                                        {{ form_row(requestForm.email) }}

                                        <small>
                                            أدخل عنوان بريدك الإلكتروني وسنرسل لك رابطًا لإعادة تعيين كلمة مرورك.
                                        </small>
                                    </div>

                                    <button class="btn btn-primary mt-4">إرسال إعادة تعين كلمة المرور</button>
                                    {{ form_end(requestForm) }}
                                    <hr>
                                    <div class="text-center">
                                        <a href="{{ path('app_login') }}">العودة إلى تسجيل الدخول</a>
                                    </div>

                                </div>
                            </div>
                            <div class="col-lg-6 d-none d-lg-block bg-login-image"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
