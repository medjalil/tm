<div class="modal fade" id="deleteArchiveModal-{{ teacher.archive.id }}" tabindex="-1" role="dialog"
     aria-labelledby="exampleModalLabel" aria-hidden="true">
    <form method="post" action="{{ path('archive_delete', {'id': teacher.archive.id}) }}">
        <input type="hidden" name="_method" value="DELETE">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">نقل من الأرشيف</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">هل تريد تحويل الأستاذ من الأرشيف؟</div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">إلغاء</button>
                    <input type="hidden" name="_token" value="{{ csrf_token('option' ~ teacher.archive.id) }}">
                    <button class="btn btn-dark"><i class="fas fa-exchange-alt"></i> نقل</button>
                </div>
            </div>
        </div>
    </form>
</div>
