{% extends 'base.html.twig' %}

{% block title %}Archive{% endblock %}

{% block body %}
    <h1>Archive</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ archive.id }}</td>
            </tr>
            <tr>
                <th>Justification</th>
                <td>{{ archive.justification }}</td>
            </tr>
            <tr>
                <th>Cooment</th>
                <td>{{ archive.cooment }}</td>
            </tr>
            <tr>
                <th>ArchivedAt</th>
                <td>{{ archive.archivedAt ? archive.archivedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('archive_index') }}">back to list</a>

    <a href="{{ path('archive_edit', {'id': archive.id}) }}">edit</a>

    {{ include('archive/_delete_form.html.twig') }}
{% endblock %}
